import { QuickStartArea } from '../../components/quickstart'

# AMPATH Forms Quickstart

AMPATH Forms is a batteries-included solution for creating, testing and validating OpenMRS form schemas.

AMPATH Forms is designed to be usable by both developers and non-developers, so users should expect to be productive with it from the get go.

<QuickStartArea />
